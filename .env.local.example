# Local CORS Proxy Configuration
# 
# The public CORS proxy (corsproxy.io) has rate limits and may block certain hosts.
# To avoid 403 errors, set up your own CORS proxy:
#
# Option 1: Use the included Cloudflare Worker proxy (see proxy/README.md)
# VITE_OWN_PROXY_URL=https://your-worker.your-subdomain.workers.dev
#
# Option 2: Use a local proxy (see OWNED_PROXY_README.md)
# VITE_OWN_PROXY_URL=http://localhost:8787
#
# Option 3: Force all requests through proxy (useful for development)
# VITE_FORCE_PROXY=true
#
# Example configuration:
# VITE_OWN_PROXY_URL=http://localhost:8787
# VITE_FORCE_PROXY=false
