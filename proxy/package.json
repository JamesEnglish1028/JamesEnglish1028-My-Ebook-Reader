{
  "name": "mebooks-proxy",
  "version": "0.1.0",
  "description": "Small proxy server for MeBooks to forward requests server-side and avoid browser CORS.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy:worker": "wrangler publish --config ./wrangler.toml"
  },
  "dependencies": {
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "node-fetch": "^2.6.7",
    "express-rate-limit": "^6.7.0",
    "body-parser": "^1.20.2"
  }
}
